# storage/longhorn/values.yaml.tmpl

# This file is generated by the kubexm engine.
# It overrides key values for the Longhorn Helm chart.

global:
  cattle:
    # This is the correct way to set a private registry for all Longhorn images.
    # The Helm chart will use this registry as a prefix for all its images.
    systemDefaultRegistry: "{{.SystemDefaultRegistry}}"

# We also set persistence.defaultClass to true as a best practice,
# making Longhorn the default StorageClass in the cluster.
persistence:
  defaultClass: true

# Note: We do not need to override individual image tags here.
# The BOM (Bill of Materials) should provide a Longhorn chart version
# that already defaults to the correct image tags for that release.
# We only override the registry.